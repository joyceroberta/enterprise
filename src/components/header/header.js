// import React from "react";
// import { useHistory } from "react-router-dom";
// import { ButtonHeader, DivH1Header, Header } from "./styled"
// import {goToHome, goToHomeAdm, goToListTripsPage, goToLoginPage} from "../../router/coordinator"

// const Headers = (props) => {
//   const history = useHistory();

//   return (
//     <div>
//       {history.location.pathname === "/home" ? (
        
//        ) : (
//         ""
//       )}

//       {history.location.pathname === "/escolherviagem" ? (
        
//       ) : (
//         ""
//       )}

//       {history.location.pathname === "/homeadm" ? (
        
//       ) : (
//         ""
//       )}

//       {history.location.pathname === "/formulario/:id" ? (
        
//       ) : (
//         ""
//       )}

//       {history.location.pathname === "/novasviagens"  ? (
        
//       ) : (
//         ""
//       )}

//       {history.location.pathname ===  "/listaviagens" ? (
       
//       ) : (
//         ""
//       )}

//       {history.location.pathname === "/aprovarinscricoes/:id" ? (
        
//       ) : (
//         ""
//       )} 
//     </div>
//   );
// }

// export default Headers;
